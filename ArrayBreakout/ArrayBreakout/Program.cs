using System;

namespace ArrayBreakout
{
    class Program
    {
        static void Main(string[] args)
        {
            double[] measurements = new double[6];
            //How do we add in measurements here? 



            int[] nums = { 12, 10, 99, 81, 40 };

            foreach(int num in nums)
            {
                Console.WriteLine(num);
            }

            string[] band = { "Jimmy", "Billy", "Theodore" };
            //We need to replace theo, how do we do that? 


        }
    }
}
